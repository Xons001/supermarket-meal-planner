# ADR 0037 — Corrección nutricional manual

La corrección requiere ADMIN, motivo, validación y escritura de historial. Se etiqueta `MANUAL`/`MANUAL_OVERRIDE` y solo otra decisión manual explícita puede sustituirla.
