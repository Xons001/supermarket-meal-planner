# ADR 0034 — Abstracción de proveedor nutricional

Se adopta `NutritionDataProvider` con búsqueda por barcode y nombre. El proveedor local es reproducible y Open Food Facts es opt-in. El dominio no depende de marcas ni APIs externas.
